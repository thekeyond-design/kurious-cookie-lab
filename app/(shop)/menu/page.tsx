import { MenuClient } from "@/components/menu/MenuClient"

export const metadata = {
  title: "The Elements | Kurious Cookie Lab",
  description: "Explore our full periodic table of cookies. Six core elements, each a carefully tested formula. Build your batch.",
}

export default function MenuPage() {
  return <MenuClient />
}
